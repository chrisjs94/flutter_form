import 'package:flutter/material.dart';

Form employeeForm(BuildContext context){
  return Form(
    child: Column(
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: const [
        
      ],
    ),
  );
}